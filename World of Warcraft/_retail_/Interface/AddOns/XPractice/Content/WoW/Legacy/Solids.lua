	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Cylinder001Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Cylinder001Red
		function class:SetActorAppearanceViaOwner(actor)
			--actor:SetModelByFileID(3088256)	--scale_obj_cylinder_001red
			--actor:SetModelByFileID(3088257)	--scale_obj_cylinder_002red
			--actor:SetModelByFileID(3088258)	--scale_obj_cylinder_003red
			actor:SetModelByFileID(3088259)	--scale_obj_cylinder_004red
			-- about ? x ? x ?
		end
	end
	
	

	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Ramp002Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Ramp002Red
		function class:SetActorAppearanceViaOwner(actor)
			actor:SetModelByFileID(3088265)	--scale_obj_ramp_002red
			-- about ? x ? x ?
		end
	end
	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Ramp003Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Ramp003Red
		function class:SetActorAppearanceViaOwner(actor)
			actor:SetModelByFileID(3088266)	--scale_obj_ramp_003red
			-- about ? x ? x ?
		end
	end
	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Ramp004Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Ramp004Red
		function class:SetActorAppearanceViaOwner(actor)
			actor:SetModelByFileID(3088267)	--scale_obj_ramp_004red
			-- about ? x ? x ?
		end
	end	
	
	
	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Rectangle001Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Rectangle001Red
		function class:SetActorAppearanceViaOwner(actor)
			actor:SetModelByFileID(3088268)	--scale_obj_rectangle_001red
			-- about ? x ? x ?
		end
	end	
	
	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Rectangle002Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Rectangle002Red
		function class:SetActorAppearanceViaOwner(actor)
			actor:SetModelByFileID(3088269)	--scale_obj_rectangle_002red
			-- about ? x ? x ?
		end
	end	

	do
		local super=XPRACTICE.WoWObject
		XPRACTICE.SOLIDS.Rectangle003Red=XPRACTICE.inheritsFrom(super)
		local class=XPRACTICE.SOLIDS.Rectangle003Red
		function class:SetActorAppearanceViaOwner(actor)
			actor:SetModelByFileID(3088270)	--scale_obj_rectangle_003red
			-- about ? x ? x ?
		end
	end	
